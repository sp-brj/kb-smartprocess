# Аудит проекта и план техдолга (2026-07-06)

Сводка полного аудита sp-kb (модель-оркестратор Fable 5 + три параллельных агента: безопасность/API,
качество кода, инфраструктура/тесты) и текущий статус каждой находки.
Источник фиксов: PR #5 `fix/security-critical` → #6 `chore/polish` → #7 `refactor/auth-layer`
(смержены в `main` и задеплоены на прод 2026-07-06).

**Общий вывод аудита:** код дисциплинирован (TS `strict`, ноль `any`/`console.log`, секреты не в git,
bcrypt, регистрация только для ADMIN, SQL параметризован), но «закрытая» база знаний была дырява
на границе публичного доступа + полностью отсутствовал CI/CD.

---

## 🔴 Критично — исправлено (PR #5)

| # | Находка | Как закрыто |
|---|---------|-------------|
| 1 | Пароль на share-ссылках — бутафория: `GET /api/share/<token>` отдавал `content` без проверки пароля, `PasswordProtectedContent` прятал текст только визуально | Серверный гейт: `/verify` ставит подписанную HttpOnly-cookie (HMAC `token:passwordHash`) — `src/lib/share-auth.ts`; проверяют API и 3 страницы |
| 2 | Stored XSS: `rehypeRaw` без санитизации в `ArticleContent` (рендерит и публичные `/share/*`), плюс `marked()` в export html/pdf | `rehype-sanitize` в `ArticleContent`, `sanitize-html` в экспорте — `src/lib/sanitize.ts` |
| 3 | Роль READER могла писать: `api-auth.ts` выдавал `read+write+admin` любой сессии | Права выводятся из роли в `api-auth.ts` |
| 4 | Нет rate limiting нигде + user enumeration и timing-атака на логине (`/api/chat` — ещё и денежный DoS) | `src/lib/rate-limit.ts` на login/share-verify/chat; единое сообщение об ошибке + bcrypt всегда |
| 5 | IDOR: любой залогиненный удалял чужие картинки/файлы | Проверка владельца при удалении |

## 🟠 Инфраструктура — исправлено (PR #6)

- **CI/CD не было вообще** → GitHub Actions `.github/workflows/ci.yml` (lint + tsc + build).
- **Мёртвый код**: 15 E2E-спеков вне `testDir` (никогда не запускались), зависимости `html-to-docx`, `dotenv`, папка `src/generated/prisma` (320 КБ) → вычищено / в `.gitignore`.
- **Три разных `generateSlug`** (статьи ≠ папки ≠ импорт) → один в `src/lib/wikilinks.ts`.
- `ArticleEditor` глотал ошибки сохранения (`try/finally` без `catch`) → обработка ошибок.
- Security-заголовки в `next.config.ts`, pin Node, актуализированы README/ARCHITECTURE.

## 🟡 Единый слой авторизации — исправлено (PR #7)

22 из 38 роутов ходили мимо `authenticateRequest()` (API-ключи и MCP не работали на половине API).
Переведено 11 роутов (versions ×4, export ×3, stats, import, share-create ×2) на
`authenticateRequest()` + проверку прав по роли.
Оставшиеся 11 (`admin`, `api-keys`, `profile`, `register`, `analytics`, `share-DELETE`) сознательно
остались на `getServerSession` — там session-only корректнее.

---

## Техдолг — закрыт (ветка `chore/tech-debt-2026-08`, 2026-08-19)

- [x] **`share-DELETE` без ownership** → поле `createdById` в `ShareLink`/`FolderShareLink`
      (миграция `20260819120000`, бэкфилл владельца из автора статьи), `canRevokeShareLink()`,
      DELETE переведён на `authenticateRequest`. Legacy-ссылки на папки отзывает только ADMIN.
- [x] **Нет пагинации и `select`** → `limit`/`offset` (по умолчанию 100, максимум 500) и явный
      `select` без `content` в `/api/articles`, счётчики в заголовках `X-Total-Count`/`X-Limit`/
      `X-Offset`; форма ответа осталась массивом ради MCP. Разбор — `src/lib/pagination.ts`.
- [x] **N+1 в reindex** → `DELETE` + пакетные `INSERT` (по 100 строк) в одной транзакции.
- [x] **Rate-limit только in-memory** → бэкенд Upstash Redis при заданных
      `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN`, откат на in-memory при сбое Redis.
      ⚠️ Переменные ещё не заведены — до этого лимит работает по-старому.
- [x] **MCP-сервер** → один общий `AsyncClient` с пулом, `verify=True` (отключается через
      `KB_TLS_VERIFY`), раздельные connect/read/write таймауты, ретраи с backoff: `GET` — на
      сетевых сбоях и 429/5xx, `POST`/`PATCH` — только когда соединение не установилось.
- [x] **~300 строк дублей Share** → общий `ShareLinkManager` (622 → 380 строк) + E2E-спек на
      пароль и отзыв ссылки.
- [x] **0 unit-тестов** → vitest, 36 тестов на чистую логику, шаг в CI.
- [x] **Рассогласование версий** → `@types/react`/`@types/react-dom` приведены к React 18.
      `eslint-config-next@16` при `next@15` оставлен намеренно (код причёсан под строгие правила).
- [x] Локальные ветки-хвосты удалены. Удалённые (`origin/feature/collapsible-sidebar`,
      `origin/chore/migrate-on-prod-build`) — ждут разрешения на push.

**Проверено:** `npm run build`, `npx tsc --noEmit`, `npm run lint`, `npm run test:unit` (36/36).
**Не проверено:** E2E — локальная БД из `.env.local` с этой машины недоступна (TCP до
пулера Supabase проходит, Postgres-соединение не устанавливается: похоже на перехват
TUN/прокси). Новые share-спеки написаны, но ни разу не прогонялись.

## Остаётся открытым

- [ ] Завести Upstash Redis и прописать переменные в Vercel — иначе новый бэкенд лимитов спит.
- [ ] Прогнать E2E (`npx playwright test --project=chromium` + `npm run test:cleanup`) там, где
      доступна БД.
- [ ] 11 роутов на `getServerSession` (`admin`, `api-keys`, `profile`, `register`, `analytics`) —
      решено оставить session-only, перепроверить при следующем изменении auth.
- [ ] Пагинация только в `/api/articles`; `/api/folders`, `/api/tags` пока отдают всё (таблицы
      маленькие) — вернуться, когда вырастут.

## Инфраструктура вне кода (на стороне Vercel/Supabase)

**Preview-деплой Vercel падает на каждом PR** — `prisma migrate deploy` не достучится до
preview-БД: `FATAL: (ENOTFOUND) tenant/user postgres.svxojrtoyjjrqcylruzg not found`.

- Прод **не затронут**: preview указывает на отдельный Supabase-проект, прод-`DATABASE_URL` валиден.
  Главная тревога аудита («preview накатит миграции с `TRUNCATE ArticleChunk` на прод») **не подтвердилась**.
- Причина: preview-проект Supabase, вероятно, поставлен на паузу за неактивность (free tier).
- Лечится одним из двух: Restore проекта в Supabase (или новый + обновить `DATABASE_URL` для
  окружения Preview), либо вернуть `scripts/vercel-build.sh` к «мигрировать только на production».
- ⚠️ Если включать branch protection с обязательным Vercel-чеком — сначала починить, иначе
  всегда-красный preview заблокирует мерж любых PR.

**Деструктивная миграция** `20260526120000_change_embedding_dim` делает `TRUNCATE ArticleChunk` —
после её накатки AI-поиск молча пустой, пока не запустить переиндексацию вручную. Авто-хука нет.

**Новая миграция `20260819120000_add_share_link_owner`** написана вручную и на локальной БД не
применялась — накатится Vercel-билдом (`prisma migrate deploy`). Она аддитивная: два nullable
столбца, два индекса, два FK и один `UPDATE` для бэкфилла.

---

## Куда развивать (из аудита + бэклога)

- **Семантический поиск отдельным эндпоинтом** `/api/search/semantic` без вызова LLM: сейчас вектор
  заперт внутри `/api/chat`, а `/api/search` — обычный ILIKE. Разблокирует MCP-агента и умный поиск в UI.
- **Права доступа на уровне папок** — логичный шаг после починки ролей (сейчас «всё или ничего»).
- **UI настроек AI-чата** — дизайн-спека готова (`docs/design-ai-settings-ui.md`), реализации нет.
- Довести **MCP-сервер** до стабильности — большинство инструментов уже обёртки над существующими API.
- Заменить ILIKE-поиск на **pg_trgm** — дёшево, база уже PostgreSQL.
- Избранное / комментарии / шаблоны статей — стандартный wiki-функционал.
